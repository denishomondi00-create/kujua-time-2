# Docker assets

These Dockerfiles package the major deployable runtimes separately:

- web
- api
- worker
- scheduler
- docs
- embed build

The goal is one image per runtime so scaling stays clean and responsibilities stay isolated.
